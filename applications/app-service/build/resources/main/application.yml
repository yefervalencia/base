spring:
  application:
    name: store-api
  data:
    mongodb:
      uri: mongodb://localhost:27017/store-db

server:
  port: 8081

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    root: INFO
