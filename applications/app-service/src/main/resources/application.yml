spring:
  application:
    name: proyecto-base
  data:
    mongodb:
      uri: mongodb://localhost:27017/proyecto-base

server:
  port: 8081

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
